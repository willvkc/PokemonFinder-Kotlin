<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/principal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="0.05dp"
    android:clickable="true"
    android:paddingStart="6dp"
    android:paddingTop="4dp"
    android:paddingEnd="6dp"
    android:paddingBottom="4dp">

    <android.support.v7.widget.CardView
        android:id="@+id/cardview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="2dp"
        android:layout_marginTop="1dp"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="4dp"
        android:padding="1dp"
        app:cardBackgroundColor="@color/amarelo"
        app:cardCornerRadius="@dimen/cardview_item">

        <ImageView
            android:id="@+id/thumbnail"
            android:layout_width="@dimen/pokemon_thumb"
            android:layout_height="@dimen/pokemon_thumb"
            android:layout_gravity="center_vertical" />
    </android.support.v7.widget.CardView>

    <TextView
        android:id="@+id/name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_gravity="center_vertical"
        android:layout_toEndOf="@id/cardview"
        android:text="TextView"
        android:textColor="@color/azul1"
        android:textSize="@dimen/item_size2"
        android:textStyle="bold" />

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/cardview"
        android:background="?android:attr/listDivider"
        android:scrollbarSize="3dp" />
</RelativeLayout>